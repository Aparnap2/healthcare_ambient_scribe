name: CI

on:
  push:
    branches: [main, feature/**]
  pull_request:
    branches: [main]

jobs:
  backend-tests:
    name: Backend Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Setup uv
        uses: astral-sh/setup-uv@v5

      - name: Install dependencies
        working-directory: ./ai_service
        # We install requirements directly to avoid the "Multiple top-level modules" build error
        run: uv pip install --system -r pyproject.toml --all-extras

      - name: Type check with mypy
        working-directory: ./ai_service
        run: uv pip install mypy && mypy . --strict

      - name: Run unit tests
        working-directory: ./ai_service
        # Setting PYTHONPATH=. ensures local modules are importable without a formal install
        run: PYTHONPATH=. uv run pytest tests/test_audio_chunking.py tests/test_fhir_converter.py tests/test_redaction.py -v

      - name: Run LLM evaluations
        if: github.event_name == 'pull_request'
        working-directory: ./ai_service
        run: PYTHONPATH=. uv run pytest tests/test_llm_evals.py -v --tb=short
        env:
          DEEPEVAL_LOG_FILE: deepeval.log

  frontend-check:
    name: Frontend Check
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Type check
        run: pnpm exec tsc --noEmit

      - name: Lint
        run: pnpm lint

      - name: Build
        run: pnpm build

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests
        run: pnpm test:e2e --project=chromium
        continue-on-error: true

  lint-all:
    name: Lint All
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Backend lint
        working-directory: ./ai_service
        run: |
          echo "Running linting in ai_service..."
          # If using ruff: uv pip install ruff && ruff check .
          
      - name: Run Frontend lint
        working-directory: ./frontend
        run: pnpm lint
