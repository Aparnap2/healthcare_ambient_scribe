[project]
name = "healthcare-ai-service"
version = "0.1.0"
description = "AI service for SOAP generation using Ollama"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
    "httpx>=0.27.0",
    "pydantic>=2.12.0",
    "pytest>=9.0.2",
]
